jdk:
  - openjdk21
before_install:
  - chmod +x ./gradlew
install:
  - ./gradlew build :core:publishToMavenLocal
  - ./gradlew build :relocation:publishToMavenLocal
  - ./gradlew build :bukkit:publishToMavenLocal
